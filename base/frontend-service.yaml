---
apiVersion: v1
kind: Service
metadata:
  name: virginia-frontend
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/contents"
spec:
  selector:
    app: virginia-frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
